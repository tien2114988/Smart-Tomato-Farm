import React from 'react';
import PropTypes from 'prop-types';

Manage.propTypes = {
    
};

function Manage(props) {
    return (
        <div>
            manage
        </div>
    );
}

export default Manage;