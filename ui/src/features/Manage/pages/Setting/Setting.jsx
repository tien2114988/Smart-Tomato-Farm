import React from 'react';
import PropTypes from 'prop-types';

Setting.propTypes = {
    
};

function Setting(props) {
    return (
        <div>
            abc
        </div>
    );
}

export default Setting;